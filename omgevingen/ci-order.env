# Waarden van de CI-omgeving van Order — geen versies.
#
# Efemeer, met eigen poorten. Payment draait hier niet: zijn plaats wordt ingenomen door
# de stub uit compose/stub.yml, die dezelfde servicenaam draagt. Order merkt het verschil
# met Test niet, en dat is het punt.

ORDER_API_POORT=7082
STUB_POORT=7083
PAYMENT_BASE_URL=http://payment-api:8081
